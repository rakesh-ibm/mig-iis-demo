CREATE DATABASE demoCRUD;

CREATE USER 'userCRUD'@'localhost' IDENTIFIED BY 'Admin12345';

GRANT SELECT, INSERT, UPDATE, DELETE, CREATE, INDEX, DROP, ALTER, CREATE TEMPORARY TABLES, LOCK TABLES ON demoCRUD.* TO 'userCRUD'@'localhost';

USE demoCRUD;

CREATE TABLE IF NOT EXISTS Student (
    StudentNo INT AUTO_INCREMENT PRIMARY KEY,
    Name VARCHAR(255) NOT NULL,
    Section INT,
    Branch VARCHAR(255) NOT NULL,
    EmailId VARCHAR(255) NOT NULL
);
